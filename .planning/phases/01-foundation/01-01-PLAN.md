---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Electron + React + TypeScript project with Vite build tooling and Tailwind CSS.

Purpose: Establish the foundational build system and project structure that all subsequent phases depend on.
Output: Working Electron app skeleton with hot reload, TypeScript compilation, and Tailwind styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@DAGENT_SPEC.md

**Tech stack (from spec):**
- Electron (desktop shell)
- React + TypeScript (UI)
- Tailwind CSS (styling)
- Vite (build tool - modern choice for Electron)

**Constraints:**
- Windows-first development
- Must support cross-platform builds later
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with electron-vite</name>
  <files>package.json, electron.vite.config.ts, tsconfig.json, tsconfig.node.json, tsconfig.web.json</files>
  <action>
    Create new electron-vite project using the React + TypeScript template:
    ```
    npm create @electron-vite/create@latest dagent-app -- --template react-ts
    ```
    Then move contents to current directory (we're in dagent/).

    After scaffolding:
    1. Update package.json name to "dagent"
    2. Update package.json description to match PROJECT.md
    3. Verify electron-vite.config.ts has correct main/preload/renderer entries

    Do NOT use electron-forge (heavier, less Vite-native). electron-vite is purpose-built for Vite + Electron.
  </action>
  <verify>npm install completes without errors, ls shows expected structure (src/main, src/preload, src/renderer)</verify>
  <done>package.json exists with electron-vite dependencies, project structure matches electron-vite template</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind CSS</name>
  <files>tailwind.config.js, postcss.config.js, src/renderer/src/assets/index.css</files>
  <action>
    Install and configure Tailwind CSS for the renderer process:
    ```
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    ```

    Configure tailwind.config.js:
    - content: ["./src/renderer/index.html", "./src/renderer/src/**/*.{js,ts,jsx,tsx}"]
    - theme.extend: {} (empty for now)

    Update src/renderer/src/assets/index.css (or create if needed):
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```

    Ensure index.css is imported in renderer entry point.
  </action>
  <verify>npm run dev starts without Tailwind errors, inspect element shows Tailwind classes compiled</verify>
  <done>Tailwind classes work in React components (test with className="bg-blue-500")</done>
</task>

<task type="auto">
  <name>Task 3: Verify development workflow</name>
  <files>package.json (scripts section)</files>
  <action>
    Verify these npm scripts work correctly:
    - `npm run dev` - starts Electron with hot reload
    - `npm run build` - builds production bundle
    - `npm run preview` - previews production build

    Test hot reload:
    1. Run `npm run dev`
    2. Modify a React component
    3. Verify change appears without full restart

    Test TypeScript:
    1. Add a type error intentionally
    2. Verify build fails with clear error
    3. Fix error, verify build succeeds

    Document any platform-specific issues (Windows paths, etc.) in comments.
  </action>
  <verify>npm run dev shows Electron window, npm run build produces dist/ folder</verify>
  <done>Dev server starts in <30s, hot reload works, TypeScript errors are caught, build produces working app</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds
- [ ] `npm run dev` opens Electron window
- [ ] Tailwind classes render correctly
- [ ] `npm run build` produces dist/ folder
- [ ] Hot reload works (change component, see update)
</verification>

<success_criteria>
- All tasks completed
- Working Electron + React + TypeScript + Tailwind development environment
- Hot reload functional
- Build produces distributable app
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
