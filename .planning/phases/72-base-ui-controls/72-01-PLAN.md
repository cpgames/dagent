---
phase: 72-base-ui-controls
plan: 01
status: planned
started: null
completed: null
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/UI/Checkbox.tsx
  - src/renderer/src/components/UI/Checkbox.css
  - src/renderer/src/components/UI/Radio.tsx
  - src/renderer/src/components/UI/Radio.css
  - src/renderer/src/components/UI/Toggle.tsx
  - src/renderer/src/components/UI/Toggle.css
  - src/renderer/src/components/UI/Slider.tsx
  - src/renderer/src/components/UI/Slider.css
  - src/renderer/src/components/UI/Select.tsx
  - src/renderer/src/components/UI/Select.css
  - src/renderer/src/components/UI/index.ts
autonomous: true
must_haves:
  truths:
    - "Checkbox toggles between checked/unchecked states"
    - "Radio buttons allow single selection within a group"
    - "Toggle switch slides between on/off states"
    - "Slider allows value selection within a range"
    - "Select dropdown shows options and allows selection"
  artifacts:
    - path: "src/renderer/src/components/UI/Checkbox.tsx"
      provides: "Checkbox component with synthwave styling"
    - path: "src/renderer/src/components/UI/Radio.tsx"
      provides: "Radio button component with group support"
    - path: "src/renderer/src/components/UI/Toggle.tsx"
      provides: "Toggle switch component"
    - path: "src/renderer/src/components/UI/Slider.tsx"
      provides: "Slider/range input component"
    - path: "src/renderer/src/components/UI/Select.tsx"
      provides: "Dropdown select component"
  key_links:
    - from: "src/renderer/src/components/UI/index.ts"
      to: "All control components"
      via: "Barrel export"
---

<objective>
Create reusable control components (Checkbox, Radio, Toggle, Slider, Select) with synthwave styling using CSS custom properties from Phase 70.

Purpose: Complete the form control primitives for the UI component library, enabling consistent styling across all interactive controls.
Output: Five new control components with CSS files, added to barrel export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v2.6-ROADMAP.md

# Prior phase provides CSS custom properties and patterns
@.planning/phases/70-theme-infrastructure/70-01-SUMMARY.md
@.planning/phases/71-base-ui-buttons/71-01-SUMMARY.md

# Reference files for patterns
@src/renderer/src/themes/synthwave.css
@src/renderer/src/components/UI/Button.tsx
@src/renderer/src/components/UI/Button.css
@src/renderer/src/components/UI/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checkbox Component</name>
  <files>src/renderer/src/components/UI/Checkbox.tsx, src/renderer/src/components/UI/Checkbox.css</files>
  <action>
Create Checkbox component with synthwave styling:

**CSS (Checkbox.css):**
- Custom checkbox appearance hiding native input
- Square box with magenta border (--border-default)
- Checked state: cyan background (--accent-primary) with white checkmark
- Hover: border glow effect (--glow-border-secondary)
- Focus-visible outline for accessibility
- Disabled state with reduced opacity
- Label text styling with proper spacing

**Component (Checkbox.tsx):**
```typescript
interface CheckboxProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
  label?: string;
  error?: boolean;
}
```
- forwardRef for DOM access
- Hidden native input with custom visual
- SVG checkmark icon for checked state
- Label click toggles checkbox
- Indeterminate state support (optional)

Reference doc/1.png: checkbox has square shape with rounded corners, cyan fill when checked, white checkmark.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Checkbox component renders with checked/unchecked/disabled states, uses CSS variables</done>
</task>

<task type="auto">
  <name>Task 2: Radio Component</name>
  <files>src/renderer/src/components/UI/Radio.tsx, src/renderer/src/components/UI/Radio.css</files>
  <action>
Create Radio component with synthwave styling:

**CSS (Radio.css):**
- Circular radio button with magenta border
- Selected state: cyan inner dot (--accent-primary)
- Hover: border glow effect
- Focus-visible outline
- Disabled state
- RadioGroup wrapper for grouping

**Component (Radio.tsx):**
```typescript
interface RadioProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
  label?: string;
}

interface RadioGroupProps {
  name: string;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  children: React.ReactNode;
  orientation?: 'horizontal' | 'vertical';
}
```
- forwardRef on Radio
- RadioGroup provides context for name/value
- Hidden native input with custom visual
- Circular outer ring, filled inner dot when selected

Reference doc/1.png: radio buttons have circle shape, cyan fill for selected state.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Radio and RadioGroup components work together, single selection within group</done>
</task>

<task type="auto">
  <name>Task 3: Toggle Component</name>
  <files>src/renderer/src/components/UI/Toggle.tsx, src/renderer/src/components/UI/Toggle.css</files>
  <action>
Create Toggle (switch) component with synthwave styling:

**CSS (Toggle.css):**
- Pill-shaped track (rounded rectangle)
- Off state: dark background (--bg-surface), subtle border
- On state: cyan background (--accent-primary) with glow
- Circular thumb that slides left/right
- Smooth transition animation
- Focus-visible outline
- Disabled state

**Component (Toggle.tsx):**
```typescript
interface ToggleProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
  label?: string;
  labelPosition?: 'left' | 'right';
}
```
- forwardRef for DOM access
- Uses checkbox input type internally
- Controlled/uncontrolled support
- Thumb slides with CSS transform

Reference doc/1.png: toggle is pill-shaped, cyan when on, thumb is white circle.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Toggle slides smoothly between on/off states with glow effect</done>
</task>

<task type="auto">
  <name>Task 4: Slider Component</name>
  <files>src/renderer/src/components/UI/Slider.tsx, src/renderer/src/components/UI/Slider.css</files>
  <action>
Create Slider (range input) component with synthwave styling:

**CSS (Slider.css):**
- Custom appearance hiding native range styling
- Track: gradient from magenta to cyan (linear-gradient)
- Progress fill: shows filled portion up to thumb position
- Thumb: circular with border and glow on hover
- Focus-visible outline on thumb
- Disabled state

**Component (Slider.tsx):**
```typescript
interface SliderProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
  showValue?: boolean;
  formatValue?: (value: number) => string;
}
```
- forwardRef for DOM access
- Uses range input type
- Optional value display
- min/max/step support from native input
- CSS custom property for fill percentage (--slider-progress)

Reference doc/1.png: slider has gradient track (magenta to cyan), circular thumb.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Slider shows gradient track with draggable thumb, value updates correctly</done>
</task>

<task type="auto">
  <name>Task 5: Select Component and Barrel Export</name>
  <files>src/renderer/src/components/UI/Select.tsx, src/renderer/src/components/UI/Select.css, src/renderer/src/components/UI/index.ts</files>
  <action>
Create Select dropdown component and update barrel export:

**CSS (Select.css):**
- Custom select appearance with dropdown arrow
- Dark background (--input-bg) with magenta border
- Focus: cyan border with glow
- Dropdown menu: elevated background with border
- Option hover: highlighted background
- Selected option: cyan accent
- Disabled state

**Component (Select.tsx):**
```typescript
interface SelectOption {
  value: string;
  label: string;
  disabled?: boolean;
}

interface SelectProps extends Omit<React.SelectHTMLAttributes<HTMLSelectElement>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
  options: SelectOption[];
  placeholder?: string;
  error?: boolean;
  errorMessage?: string;
}
```
- forwardRef for DOM access
- Uses native select element for accessibility
- Custom chevron icon
- Placeholder option support
- Error state styling

**Update index.ts:**
Add exports for all new components:
- Checkbox, CheckboxProps
- Radio, RadioProps, RadioGroup, RadioGroupProps
- Toggle, ToggleProps
- Slider, SliderProps
- Select, SelectProps, SelectOption
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Select component works with options, all controls exported from index.ts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Checkbox.tsx exists with checked/unchecked/disabled states
- [ ] Radio.tsx and RadioGroup exist with single selection
- [ ] Toggle.tsx slides between on/off with animation
- [ ] Slider.tsx shows gradient track with draggable thumb
- [ ] Select.tsx renders dropdown with options
- [ ] All components use CSS custom properties (no hardcoded colors)
- [ ] All components exported from index.ts
</verification>

<success_criteria>

- All 5 control components created with CSS files
- All components follow Phase 71 patterns (forwardRef, size prop, CSS variables)
- Barrel export updated with all new components
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/72-base-ui-controls/72-01-SUMMARY.md`
</output>
