---
phase: 49-kanban-ui-polish
plan: 01
title: Kanban UI Polish
scope: small
duration: ~5 min
tests: none
---

# Plan: Kanban UI Polish

## Objective

Polish Kanban board UI with improved padding, spacing, scrollbar styling, and empty state presentation.

Purpose: Complete v2.1 milestone with consistent, polished Kanban UI
Output: Refined Kanban view with better visual consistency

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-kanban-feature-status/47-01-SUMMARY.md
@.planning/phases/48-feature-start-button/48-01-SUMMARY.md
@src/renderer/src/views/KanbanView.tsx
@src/renderer/src/components/Kanban/KanbanColumn.tsx
@src/renderer/src/components/Kanban/FeatureCard.tsx

**Tech stack available:** React, Tailwind CSS v4, TypeScript
**Established patterns:**
- Tailwind utility classes for styling
- Status color mapping in components
- Group hover effects on cards

**Constraining decisions:**
- Phase 47: Feature status computed from task states
- Phase 48: Start button appears on hover for startable features

## Tasks

### Task 1: Polish FeatureCard Layout

**Files:** `src/renderer/src/components/Kanban/FeatureCard.tsx`

**Action:**
1. Remove empty bottom row when no Archive button (completed features show Archive, others don't):
   - Current: `<div className="flex items-center justify-end mt-2">` always renders with empty space
   - Fix: Only render bottom section when Archive button should show
2. Adjust card padding from `p-4` to `p-3` for tighter cards
3. Add subtle shadow on hover: `hover:shadow-lg hover:shadow-black/20`

**Verify:** `npm run typecheck` passes

**Done:**
- Cards no longer have empty bottom space when no Archive button
- Card padding is tighter
- Hover shows subtle shadow

### Task 2: Polish KanbanColumn Styling

**Files:** `src/renderer/src/components/Kanban/KanbanColumn.tsx`

**Action:**
1. Add styled scrollbar to cards container:
   - Use Tailwind scrollbar classes: `scrollbar-thin scrollbar-thumb-gray-700 scrollbar-track-transparent`
   - Note: Tailwind v4 may need custom CSS if plugin not available - use `[&::-webkit-scrollbar]` classes
2. Improve empty state styling:
   - Add border: `border border-dashed border-gray-700 rounded-lg`
   - Center text vertically in available space
3. Add column background: `bg-gray-900/30 rounded-lg p-3` for visual separation

**Verify:** `npm run typecheck` passes

**Done:**
- Scrollbar is styled with gray thumb
- Empty state has dashed border
- Columns have subtle background

### Task 3: Polish KanbanView Container

**Files:** `src/renderer/src/views/KanbanView.tsx`

**Action:**
1. Increase container padding from `p-4` to `p-6` for better edge spacing
2. Add `pb-2` to account for scrollbar gutter at bottom
3. Add horizontal scroll styling for column overflow:
   - `scrollbar-thin scrollbar-thumb-gray-700 scrollbar-track-transparent`

**Verify:** `npm run build` passes

**Done:**
- Container has comfortable edge padding
- Horizontal scrollbar styled consistently
- No layout issues at any viewport size

## Verification

- [ ] `npm run build` succeeds without errors
- [ ] Cards show tighter padding without empty space
- [ ] Columns have subtle background separation
- [ ] Empty columns show dashed border placeholder
- [ ] Scrollbars are styled gray (not default browser)

## Success Criteria

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Kanban UI looks polished and consistent
- Phase 49 complete, v2.1 milestone complete

## Output

After completion, create `.planning/phases/49-kanban-ui-polish/49-01-SUMMARY.md`
