---
phase: 79-chat-views-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/views/AgentsView.tsx
  - src/renderer/src/views/AgentsView.css
  - src/renderer/src/components/Agents/AgentConfigPanel.tsx
  - src/renderer/src/components/Agents/AgentConfigPanel.css
  - src/renderer/src/components/Agents/AgentLogsPanel.tsx
  - src/renderer/src/components/Agents/AgentLogsPanel.css
autonomous: true
must_haves:
  truths:
    - "AgentsView uses CSS custom properties for all colors"
    - "Agent cards have neon border glow when selected"
    - "Status indicators use semantic theme colors"
    - "AgentConfigPanel uses UI components for inputs"
    - "AgentLogsPanel filter buttons match synthwave styling"
  artifacts:
    - path: "src/renderer/src/views/AgentsView.css"
      provides: "Agents view styling with theme variables"
      min_lines: 50
    - path: "src/renderer/src/components/Agents/AgentConfigPanel.css"
      provides: "Agent config panel styling"
      min_lines: 60
    - path: "src/renderer/src/components/Agents/AgentLogsPanel.css"
      provides: "Agent logs panel styling"
      min_lines: 40
  key_links:
    - from: "src/renderer/src/views/AgentsView.tsx"
      to: "AgentsView.css"
      via: "CSS import"
      pattern: "import.*AgentsView\\.css"
---

<objective>
Migrate AgentsView, AgentConfigPanel, and AgentLogsPanel components to use CSS custom properties with synthwave theming.

Purpose: Continue the v2.6 Synthwave UI Redesign by converting agent-related components from Tailwind classes to CSS custom properties.
Output: Updated agent components with dedicated CSS files using theme variables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/renderer/src/views/AgentsView.tsx
@src/renderer/src/components/Agents/AgentConfigPanel.tsx
@src/renderer/src/components/Agents/AgentLogsPanel.tsx
@src/renderer/src/components/UI/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AgentsView component</name>
  <files>src/renderer/src/views/AgentsView.tsx, src/renderer/src/views/AgentsView.css</files>
  <action>
1. Create AgentsView.css with:
   - `.agents-view` container with flex column, padding
   - `.agents-view__header` with title and description
   - `.agents-view__title` using --text-primary
   - `.agents-view__description` using --text-secondary
   - `.agents-view__tabs` for tab button container with border-bottom
   - `.agents-view__tab` for tab buttons with transition
   - `.agents-view__tab--active` with --accent-primary border indicator
   - `.agents-view__grid` for agent cards grid layout
   - `.agents-view__card` with --bg-surface, border, hover and selected states
   - `.agents-view__card--selected` with --accent-primary border glow
   - `.agents-view__card--disabled` with reduced opacity
   - `.agents-view__card-header` with name and status
   - `.agents-view__card-name` using --text-primary
   - `.agents-view__card-description` using --text-secondary, line-clamp
   - `.agents-view__card-task` for current task display with --accent-primary
   - `.agents-view__card-badge` for disabled badge
   - `.agents-view__status` for status indicator dot and label
   - `.agents-view__status-dot--idle` with --color-success
   - `.agents-view__status-dot--busy` with --color-warning and pulse
   - `.agents-view__status-dot--offline` with --text-muted
   - `.agents-view__loading` for loading state

2. Update AgentsView.tsx:
   - Add CSS import
   - Replace Tailwind classes with BEM-style CSS classes
   - Remove inline StatusIndicator function, use CSS classes instead
  </action>
  <verify>npm run build passes</verify>
  <done>AgentsView uses CSS custom properties, cards have neon glow when selected, status dots use semantic colors</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AgentConfigPanel component</name>
  <files>src/renderer/src/components/Agents/AgentConfigPanel.tsx, src/renderer/src/components/Agents/AgentConfigPanel.css</files>
  <action>
1. Create AgentConfigPanel.css with:
   - `.agent-config` container with --bg-surface, border, padding
   - `.agent-config__header` with flex layout, name and close button
   - `.agent-config__title` using --text-primary
   - `.agent-config__close-btn` with hover state
   - `.agent-config__status-badge` for status badges with appropriate colors
   - `.agent-config__activity` for current task display with --accent-primary border
   - `.agent-config__field` for form field containers
   - `.agent-config__label` using --text-secondary
   - `.agent-config__hint` using --text-muted
   - `.agent-config__tools` for tools list with flex-wrap
   - `.agent-config__tool-badge` with --bg-elevated background
   - `.agent-config__actions` with border-top, button alignment
   - `.agent-config__reset-btn` with ghost styling
   - `.agent-config__save-btn` with primary/disabled states

2. Update AgentConfigPanel.tsx:
   - Add CSS import
   - Import Input, Textarea, Select, Checkbox, Button from '../UI'
   - Replace native inputs with UI components
   - Replace Tailwind classes with BEM-style CSS classes
   - Remove inline StatusBadge and XIcon functions, use CSS
  </action>
  <verify>npm run build passes</verify>
  <done>AgentConfigPanel uses UI components for inputs, CSS custom properties for styling</done>
</task>

<task type="auto">
  <name>Task 3: Migrate AgentLogsPanel component</name>
  <files>src/renderer/src/components/Agents/AgentLogsPanel.tsx, src/renderer/src/components/Agents/AgentLogsPanel.css</files>
  <action>
1. Create AgentLogsPanel.css with:
   - `.agent-logs` container with flex column
   - `.agent-logs__filters` with border-bottom, flex layout
   - `.agent-logs__filter-label` using --text-secondary
   - `.agent-logs__filter-btn` with inactive/active states
   - `.agent-logs__filter-btn--active` with --accent-primary background
   - `.agent-logs__live-indicator` with --color-success and pulse animation
   - `.agent-logs__entries` scrollable container
   - `.agent-logs__empty` for empty/loading states
   - `.agent-logs__entry` with --bg-surface, border, padding
   - `.agent-logs__entry-header` with flex layout for metadata
   - `.agent-logs__timestamp` with monospace, --text-muted
   - `.agent-logs__agent-badge` for agent type badges:
     - `--harness` with purple
     - `--task` with blue (--accent-primary)
     - `--merge` with green (--color-success)
     - `--pm` with indigo (--accent-secondary)
   - `.agent-logs__type-badge` for log type badges:
     - `--intention` with gray
     - `--approval` with green
     - `--rejection` with red
     - `--modification` with yellow
     - `--action` with blue
     - `--error` with dark red
   - `.agent-logs__task-id` using --text-muted
   - `.agent-logs__content` using --text-primary

2. Update AgentLogsPanel.tsx:
   - Add CSS import
   - Replace Tailwind classes with BEM-style CSS classes
   - Remove AGENT_COLORS and TYPE_COLORS objects, use CSS classes
  </action>
  <verify>npm run build passes</verify>
  <done>AgentLogsPanel uses CSS custom properties, filter buttons and badges match synthwave aesthetic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] AgentsView.tsx imports and uses AgentsView.css
- [ ] AgentConfigPanel.tsx imports and uses AgentConfigPanel.css
- [ ] AgentLogsPanel.tsx imports and uses AgentLogsPanel.css
- [ ] All Tailwind color classes replaced with CSS custom properties
- [ ] AgentConfigPanel uses UI Input, Textarea, Select, Checkbox, Button components
- [ ] Status indicators use semantic colors (green=idle, yellow=busy, gray=offline)
- [ ] Log entry badges have distinct colors matching established patterns
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Agent components match synthwave aesthetic established in prior phases
</success_criteria>

<output>
After completion, create `.planning/phases/79-chat-views-migration/79-03-SUMMARY.md`
</output>
