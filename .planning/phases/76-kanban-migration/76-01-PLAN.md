---
phase: 76-kanban-migration
plan: 01
status: planned
started: null
completed: null
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/views/KanbanView.tsx
  - src/renderer/src/components/Kanban/KanbanColumn.tsx
  - src/renderer/src/components/Kanban/KanbanColumn.css
  - src/renderer/src/components/Kanban/FeatureCard.tsx
  - src/renderer/src/components/Kanban/FeatureCard.css
autonomous: true
must_haves:
  truths:
    - "Kanban columns have neon-bordered appearance with glass effect"
    - "Feature cards use Card component hover states with magenta glow"
    - "Status colors use theme semantic variables"
    - "Count badges use Badge component styling"
    - "Action buttons use theme hover colors"
  artifacts:
    - path: "src/renderer/src/components/Kanban/KanbanColumn.css"
      provides: "Synthwave column styling"
    - path: "src/renderer/src/components/Kanban/FeatureCard.css"
      provides: "Synthwave card styling"
  key_links:
    - from: "KanbanColumn.tsx"
      to: "KanbanColumn.css"
      via: "CSS import and class usage"
    - from: "FeatureCard.tsx"
      to: "FeatureCard.css"
      via: "CSS import and class usage"
---

<objective>
Migrate Kanban view components to synthwave theme with CSS custom properties.

Purpose: Apply synthwave aesthetic to the Kanban board (columns, feature cards, status badges) consistent with the established theme system.
Output: Updated KanbanView.tsx, KanbanColumn.tsx/css, and FeatureCard.tsx/css using theme variables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Prior phases provide CSS custom properties and components
@.planning/phases/70-theme-infrastructure/70-01-SUMMARY.md
@.planning/phases/75-layout-migration/75-01-SUMMARY.md

# Reference files for patterns and variables
@src/renderer/src/themes/synthwave.css
@src/renderer/src/components/UI/Card.css
@src/renderer/src/components/UI/Badge.css

# Current Kanban components to migrate
@src/renderer/src/views/KanbanView.tsx
@src/renderer/src/components/Kanban/KanbanColumn.tsx
@src/renderer/src/components/Kanban/FeatureCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KanbanView with Theme Variables</name>
  <files>src/renderer/src/views/KanbanView.tsx</files>
  <action>
Update KanbanView.tsx to use theme CSS variables:

**1. Loading state:**
Replace `text-gray-400` with theme variable:
```tsx
<p className="text-[var(--text-muted)]">Loading features...</p>
```

**2. Main container:**
Replace Tailwind padding and scrollbar styling with CSS variables:
- Keep the flex layout structure
- Update scrollbar styling to use theme colors:
  - `[&::-webkit-scrollbar-thumb]:bg-[var(--border-default)]`
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>KanbanView uses theme variables for text colors and scrollbars</done>
</task>

<task type="auto">
  <name>Task 2: Create KanbanColumn CSS and Update Component</name>
  <files>src/renderer/src/components/Kanban/KanbanColumn.tsx, src/renderer/src/components/Kanban/KanbanColumn.css</files>
  <action>
Create CSS file and update component to use synthwave styling:

**CSS (KanbanColumn.css):**
```css
/**
 * KanbanColumn Component Styles
 * Glass-effect columns with neon header colors
 */

.kanban-column {
  flex: 1;
  min-width: 250px;
  max-width: 350px;
  display: flex;
  flex-direction: column;
  background: var(--bg-surface);
  border-radius: var(--radius-lg);
  border: 1px solid var(--border-default);
  padding: var(--space-md);
}

.kanban-column__header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-sm);
  margin-bottom: var(--space-md);
}

.kanban-column__title {
  font-size: 0.875rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

/* Status-specific title colors */
.kanban-column__title--not_started {
  color: var(--accent-primary);
}

.kanban-column__title--in_progress {
  color: var(--color-warning);
}

.kanban-column__title--needs_attention {
  color: var(--color-error);
}

.kanban-column__title--completed {
  color: var(--color-success);
}

.kanban-column__count {
  background: var(--bg-hover);
  color: var(--text-secondary);
  font-size: 0.75rem;
  font-weight: 500;
  padding: 0.125rem var(--space-sm);
  border-radius: var(--radius-full);
}

.kanban-column__cards {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  overflow-y: auto;
  padding-right: var(--space-xs);
}

/* Custom scrollbar */
.kanban-column__cards::-webkit-scrollbar {
  width: 6px;
}

.kanban-column__cards::-webkit-scrollbar-thumb {
  background: var(--border-default);
  border-radius: var(--radius-full);
}

.kanban-column__cards::-webkit-scrollbar-track {
  background: transparent;
}

/* Empty state */
.kanban-column__empty {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px dashed var(--border-subtle);
  border-radius: var(--radius-md);
}

.kanban-column__empty-text {
  font-size: 0.875rem;
  color: var(--text-muted);
}
```

**Component updates:**
- Import CSS file
- Replace Tailwind classes with CSS classes
- Use status-based title class: `kanban-column__title--${status}`
- Remove inline style for titleColor, use CSS classes instead
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>KanbanColumn uses CSS custom properties with status-colored titles</done>
</task>

<task type="auto">
  <name>Task 3: Create FeatureCard CSS and Update Component</name>
  <files>src/renderer/src/components/Kanban/FeatureCard.tsx, src/renderer/src/components/Kanban/FeatureCard.css</files>
  <action>
Create CSS file and update component to use synthwave styling:

**CSS (FeatureCard.css):**
```css
/**
 * FeatureCard Component Styles
 * Feature cards with status-colored borders and glow effects
 */

.feature-card {
  background: var(--bg-elevated);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  cursor: pointer;
  transition: all var(--transition-fast);
  border: 1px solid var(--border-default);
  border-left-width: 4px;
}

.feature-card:hover {
  border-color: var(--card-hover-border);
  box-shadow: var(--glow-border-secondary);
}

.feature-card:focus-visible {
  outline: 2px solid var(--accent-primary);
  outline-offset: 2px;
}

/* Status border colors */
.feature-card--not_started {
  border-left-color: var(--accent-primary);
}

.feature-card--in_progress {
  border-left-color: var(--color-warning);
}

.feature-card--needs_attention {
  border-left-color: var(--color-error);
}

.feature-card--completed {
  border-left-color: var(--color-success);
}

.feature-card__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--space-sm);
}

.feature-card__title {
  color: var(--text-primary);
  font-weight: 500;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
}

.feature-card__actions {
  display: flex;
  align-items: center;
  gap: var(--space-xs);
}

/* Action buttons - visible on hover */
.feature-card__action-btn {
  opacity: 0;
  padding: var(--space-xs);
  margin: calc(-1 * var(--space-xs));
  background: transparent;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  transition: all var(--transition-fast);
  border-radius: var(--radius-sm);
}

.feature-card:hover .feature-card__action-btn {
  opacity: 1;
}

.feature-card__action-btn:hover {
  color: var(--text-secondary);
  background: var(--bg-hover);
}

.feature-card__action-btn--start:hover {
  color: var(--color-success);
}

.feature-card__action-btn--delete:hover {
  color: var(--color-error);
}

.feature-card__action-btn--loading {
  opacity: 1;
  color: var(--color-success);
  cursor: wait;
}

/* Icon sizing */
.feature-card__icon {
  width: 1rem;
  height: 1rem;
}

/* Footer actions (merge, archive) */
.feature-card__footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--space-md);
  margin-top: var(--space-sm);
}

.feature-card__merge-btn {
  display: flex;
  align-items: center;
  gap: var(--space-xs);
  font-size: 0.875rem;
  color: var(--accent-primary);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: color var(--transition-fast);
}

.feature-card__merge-btn:hover {
  color: var(--accent-primary-bright);
}

.feature-card__archive-btn {
  font-size: 0.875rem;
  color: var(--text-muted);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: color var(--transition-fast);
}

.feature-card__archive-btn:hover {
  color: var(--text-primary);
}

/* Merge dropdown */
.feature-card__dropdown {
  position: absolute;
  right: 0;
  top: 100%;
  margin-top: var(--space-xs);
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-lg);
  z-index: var(--z-dropdown);
  min-width: 120px;
  padding: var(--space-xs) 0;
}

.feature-card__dropdown-item {
  width: 100%;
  text-align: left;
  padding: var(--space-sm) var(--space-md);
  font-size: 0.875rem;
  color: var(--text-secondary);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background var(--transition-fast);
}

.feature-card__dropdown-item:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
}
```

**Component updates:**
- Import CSS file
- Replace Tailwind classes with CSS classes
- Use status-based card class: `feature-card--${feature.status}`
- Keep same functionality, update only styling classes
- Remove inline style for borderColor
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>FeatureCard uses CSS custom properties with status borders and glow effects</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] KanbanView.tsx uses theme variables
- [ ] KanbanColumn.css exists with CSS custom properties
- [ ] KanbanColumn.tsx uses CSS classes instead of Tailwind
- [ ] FeatureCard.css exists with CSS custom properties
- [ ] FeatureCard.tsx uses CSS classes instead of Tailwind
- [ ] Status colors use theme semantic variables (not hardcoded hex values)
</verification>

<success_criteria>

- Kanban columns have glass effect background with theme borders
- Column titles use theme status colors (cyan, warning, error, success)
- Feature cards have status-colored left border
- Cards glow magenta on hover
- Action buttons use theme hover colors
- All Tailwind color classes replaced with CSS custom properties
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/76-kanban-migration/76-01-SUMMARY.md`
</output>
