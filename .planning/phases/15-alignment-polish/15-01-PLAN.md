---
phase: 15-alignment-polish
plan: 01
type: execute
---

<objective>
Fix layout alignment issues in views and status bar for consistent spacing and sizing.

Purpose: Ensure all UI components fill their containers correctly and display information properly.
Output: Fixed ContextView textarea sizing, complete StatusBar layout with project path, responsive handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior Summaries:**
@.planning/phases/14-git-branch-management/14-02-SUMMARY.md
@.planning/phases/11-layout-restructure/11-03-SUMMARY.md

**Existing Files:**
@src/renderer/src/views/ContextView.tsx
@src/renderer/src/components/Layout/StatusBar.tsx
@src/renderer/src/App.tsx

**Tech stack available:** electron-vite, Tailwind v4, Zustand, React Flow
**Established patterns:** Three-column layout (sidebar | main | chat), status bar at bottom
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ContextView textarea fill</name>
  <files>src/renderer/src/views/ContextView.tsx</files>
  <action>
    Fix the textarea to properly fill the available vertical space:
    1. The outer container already has `h-full` and `flex flex-col`
    2. The inner div wrapping textarea has `flex-1 min-h-0 flex flex-col`
    3. The textarea has `flex-1 w-full min-h-0`

    The issue is the textarea needs explicit height control. Change:
    - Remove `min-h-0` from textarea (redundant)
    - Add `h-full` to the inner container div around textarea
    - Ensure textarea parent allows shrinking with `overflow-hidden`

    This ensures the textarea expands to fill available space while respecting
    the header and footer sections.
  </action>
  <verify>npm run typecheck passes</verify>
  <done>ContextView textarea fills available vertical space correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add project path to StatusBar left section</name>
  <files>src/renderer/src/components/Layout/StatusBar.tsx, src/renderer/src/App.tsx</files>
  <action>
    Per 11-02 milestone requirements, StatusBar should show project path on left.

    1. Update StatusBar to accept a `projectPath` prop or use useProjectStore directly
    2. Add a left section to StatusBar that displays truncated project path
    3. Layout should be: [project path] ... [git status] [auth indicator]
    4. Truncate path from left if too long (show ...last/two/parts)
    5. Add tooltip showing full path on hover

    The StatusBar currently only has right-aligned content. Need to add left section.
  </action>
  <verify>npm run typecheck passes</verify>
  <done>StatusBar shows project path on left, auth/git on right</done>
</task>

<task type="auto">
  <name>Task 3: Status bar responsive handling</name>
  <files>src/renderer/src/components/Layout/StatusBar.tsx, src/renderer/src/components/Git/GitStatus.tsx</files>
  <action>
    Add responsive handling for long content:
    1. StatusBar: Use `justify-between` with left and right sections
    2. Project path: Use `truncate` class, max-width constraint
    3. Git branch name: Add `truncate` with max-width (e.g., max-w-32)
    4. Change counts: Already compact, keep as-is
    5. Ensure all sections have `overflow-hidden` to prevent layout breaking

    Test with long project paths and branch names to ensure no horizontal overflow.
  </action>
  <verify>npm run typecheck passes</verify>
  <done>StatusBar handles long project paths and branch names without breaking layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` succeeds without errors
- [ ] ContextView textarea fills available space
- [ ] StatusBar shows project path on left
- [ ] Long paths/branch names are truncated gracefully
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No layout overflow issues
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/15-alignment-polish/15-01-SUMMARY.md`
</output>
