---
phase: 80-project-auth-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/Project/ProjectSelector.tsx
  - src/renderer/src/components/Project/ProjectSelector.css
  - src/renderer/src/components/Project/ProjectSelectionDialog.tsx
  - src/renderer/src/components/Project/ProjectSelectionDialog.css
  - src/renderer/src/components/Project/NewProjectDialog.tsx
  - src/renderer/src/components/Project/NewProjectDialog.css
  - src/renderer/src/components/Project/GitInitDialog.tsx
  - src/renderer/src/components/Project/GitInitDialog.css
  - src/renderer/src/components/Auth/AuthStatusIndicator.tsx
  - src/renderer/src/components/Auth/AuthStatusIndicator.css
  - src/renderer/src/components/Auth/AuthDialog.tsx
  - src/renderer/src/components/Auth/AuthDialog.css
autonomous: true
must_haves:
  truths:
    - "Project selector dropdown uses synthwave styling"
    - "Project selection dialog uses Dialog component and synthwave colors"
    - "New project dialog uses UI components (Dialog, Input, Button)"
    - "Git init dialog uses synthwave colors with warning states"
    - "Auth status indicator uses theme colors (green for authenticated, yellow for not)"
    - "Auth dialog uses Dialog component with synthwave form styling"
  artifacts:
    - path: "src/renderer/src/components/Project/ProjectSelector.css"
      provides: "Synthwave styling for project dropdown"
    - path: "src/renderer/src/components/Project/ProjectSelectionDialog.css"
      provides: "Synthwave styling for project selection"
    - path: "src/renderer/src/components/Project/NewProjectDialog.css"
      provides: "Synthwave styling for new project form"
    - path: "src/renderer/src/components/Project/GitInitDialog.css"
      provides: "Synthwave styling for git init warning"
    - path: "src/renderer/src/components/Auth/AuthStatusIndicator.css"
      provides: "Synthwave styling for auth status"
    - path: "src/renderer/src/components/Auth/AuthDialog.css"
      provides: "Synthwave styling for auth dialog"
  key_links:
    - from: "ProjectSelectionDialog.tsx"
      to: "UI/Dialog"
      via: "import { Dialog, DialogHeader, DialogBody, DialogFooter }"
    - from: "NewProjectDialog.tsx"
      to: "UI/Dialog, UI/Input, UI/Button"
      via: "imports from ../UI"
    - from: "AuthDialog.tsx"
      to: "UI/Dialog, UI/Input, UI/Button, UI/Radio"
      via: "imports from ../UI"
---

<objective>
Migrate Project and Auth components to synthwave theme with CSS custom properties.

Purpose: Complete the UI migration by updating remaining project selection and authentication components.
Output: All 6 components migrated with CSS files using theme variables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v2.6-ROADMAP.md

# Prior phase summaries (migration patterns)
@.planning/phases/78-dialog-migration/78-01-SUMMARY.md
@.planning/phases/79-chat-views-migration/79-01-SUMMARY.md

# Source files to migrate
@src/renderer/src/components/Project/ProjectSelector.tsx
@src/renderer/src/components/Project/ProjectSelectionDialog.tsx
@src/renderer/src/components/Project/NewProjectDialog.tsx
@src/renderer/src/components/Project/GitInitDialog.tsx
@src/renderer/src/components/Auth/AuthStatusIndicator.tsx
@src/renderer/src/components/Auth/AuthDialog.tsx

# UI components available
@src/renderer/src/components/UI/index.ts
@src/renderer/src/components/UI/Dialog.tsx
@src/renderer/src/components/UI/Button.tsx
@src/renderer/src/components/UI/Input.tsx
@src/renderer/src/components/UI/Radio.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Project components</name>
  <files>
    src/renderer/src/components/Project/ProjectSelector.tsx
    src/renderer/src/components/Project/ProjectSelector.css
    src/renderer/src/components/Project/ProjectSelectionDialog.tsx
    src/renderer/src/components/Project/ProjectSelectionDialog.css
    src/renderer/src/components/Project/NewProjectDialog.tsx
    src/renderer/src/components/Project/NewProjectDialog.css
    src/renderer/src/components/Project/GitInitDialog.tsx
    src/renderer/src/components/Project/GitInitDialog.css
  </files>
  <action>
Migrate all 4 Project components to use CSS custom properties and UI components:

**ProjectSelector.tsx:**
- Replace Tailwind classes with BEM-style CSS classes
- Create ProjectSelector.css with synthwave styling
- Dropdown should use --bg-elevated, --border-accent, --text-primary
- Hover states should use --bg-hover with subtle glow

**ProjectSelectionDialog.tsx:**
- Import and use Dialog, DialogHeader, DialogBody, DialogFooter from ../UI
- Import Button from ../UI for action buttons
- Replace inline styles with CSS classes
- Create ProjectSelectionDialog.css for custom elements
- Recent projects list should use --bg-surface, folder icons use --accent-secondary
- Open Folder button uses blue accent, Create New uses green accent (--color-success)
- Loading spinner uses --accent-primary (cyan)
- Error display uses --color-error styling

**NewProjectDialog.tsx:**
- Import Dialog, DialogHeader, DialogBody, DialogFooter, Input, Button from ../UI
- The dialog is already mostly migrated (check Phase 78), but verify CSS file exists
- If CSS file missing, create NewProjectDialog.css with form field styling
- Labels use --text-secondary, help text uses --text-muted

**GitInitDialog.tsx:**
- Import Dialog, DialogHeader, DialogBody, DialogFooter, Button from ../UI
- Create GitInitDialog.css with synthwave warning styling
- Warning icon container uses --color-warning at 20% opacity
- Initialize Git button uses --accent-primary (primary action)
- Open Another Project and Refresh buttons use ghost variant
- Loading spinner uses --accent-primary

For all components:
- Add `import './ComponentName.css'` at top
- Use BEM naming: .component-name, .component-name__element, .component-name--modifier
- Remove all Tailwind color classes (bg-gray-*, text-gray-*, etc.)
- Keep Tailwind utility classes only for layout (flex, items-center, gap-*, w-*, etc.)
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>All 4 Project components use CSS custom properties and synthwave colors</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Auth components</name>
  <files>
    src/renderer/src/components/Auth/AuthStatusIndicator.tsx
    src/renderer/src/components/Auth/AuthStatusIndicator.css
    src/renderer/src/components/Auth/AuthDialog.tsx
    src/renderer/src/components/Auth/AuthDialog.css
  </files>
  <action>
Migrate both Auth components to use CSS custom properties and UI components:

**AuthStatusIndicator.tsx:**
- Replace Tailwind classes with BEM-style CSS classes
- Create AuthStatusIndicator.css with synthwave styling
- Status dot colors: green (--color-success), yellow (--color-warning)
- Text colors: --text-secondary for checking, corresponding status color for states
- Hover state uses --bg-hover

**AuthDialog.tsx:**
- Import Dialog, DialogHeader, DialogBody, DialogFooter, Button, Input from ../UI
- Import RadioGroup from ../UI for credential type selection (api_key/oauth)
- Create AuthDialog.css for custom sections
- SDK status section:
  - Available: green border (--color-success at 30% opacity), green text
  - Not available: yellow text (--color-warning)
- Credential display section uses --bg-surface with border
- Status dot uses same colors as indicator
- Form labels use --text-secondary
- Help text link uses --accent-primary with hover glow
- Error display uses --color-error styling

For both components:
- Add `import './ComponentName.css'` at top
- Use BEM naming convention
- Remove all Tailwind color classes
- Keep Tailwind utility classes only for layout
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>Both Auth components use CSS custom properties and synthwave colors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] All 6 components have corresponding CSS files
- [ ] All components import their CSS files
- [ ] No Tailwind color classes remain (no bg-gray-*, text-gray-*, border-gray-*, etc.)
- [ ] All dialogs use UI/Dialog component
- [ ] Buttons use UI/Button component
- [ ] Inputs use UI/Input component
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Project and Auth components display with synthwave aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/80-project-auth-migration/80-01-SUMMARY.md`
</output>
