---
phase: 09-feature-creation
plan: 02
type: execute
---

<objective>
Create NewFeatureDialog component for collecting feature name from user.

Purpose: Provide UI for users to enter a name when creating a new feature.
Output: Reusable dialog component following existing AuthDialog patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./09-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Prior plan:**
@.planning/phases/09-feature-creation/09-01-SUMMARY.md

**Key files:**
@src/renderer/src/components/Auth/AuthDialog.tsx (pattern reference)
@src/renderer/src/App.tsx

**Patterns established:**
- Dialogs use fixed backdrop with centered content
- Dark theme: bg-gray-800, border-gray-700, text-white
- Form inputs follow AuthDialog styling
- Error state displayed below input
- Cancel and Submit buttons with proper styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NewFeatureDialog component</name>
  <files>src/renderer/src/components/Feature/NewFeatureDialog.tsx</files>
  <action>
Create a new dialog component at src/renderer/src/components/Feature/NewFeatureDialog.tsx:

1. Props interface:
   - isOpen: boolean
   - onClose: () => void
   - onSubmit: (name: string) => Promise<void>

2. State:
   - name: string (input value)
   - error: string | null (validation errors)
   - isSubmitting: boolean (loading state)

3. Validation:
   - Name must not be empty
   - Name must be at least 2 characters

4. UI structure (follow AuthDialog pattern):
   - Fixed backdrop (bg-black/50)
   - Centered card (bg-gray-800, rounded-lg, max-w-md)
   - Header: "Create New Feature" with X close button
   - Input field for feature name with label
   - Error message display below input
   - Footer: Cancel button (gray) and Create button (blue)
   - Create button shows loading state when isSubmitting

5. Behavior:
   - Prevent event propagation on card click
   - Clear form on close
   - Clear error when user types
   - Submit calls onSubmit prop, handles errors
   - Close on successful submit
  </action>
  <verify>TypeScript compiles: `npm run typecheck`</verify>
  <done>NewFeatureDialog.tsx exists with proper validation and styling</done>
</task>

<task type="auto">
  <name>Task 2: Create Feature components barrel export</name>
  <files>src/renderer/src/components/Feature/index.ts</files>
  <action>
Create barrel export file at src/renderer/src/components/Feature/index.ts:

```typescript
export { NewFeatureDialog } from './NewFeatureDialog'
```

This follows the pattern established by components/Auth/index.ts.
  </action>
  <verify>TypeScript compiles: `npm run typecheck`</verify>
  <done>Feature components exportable from '@/components/Feature'</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] NewFeatureDialog component created with proper props
- [ ] Validation prevents empty/short names
- [ ] Loading state shown during submission
- [ ] Error state displayed for validation failures
</verification>

<success_criteria>
- NewFeatureDialog component implemented
- Follows AuthDialog styling patterns
- Proper validation (non-empty, min 2 chars)
- Loading and error states handled
- Barrel export available
</success_criteria>

<output>
After completion, create `.planning/phases/09-feature-creation/09-02-SUMMARY.md`
</output>
