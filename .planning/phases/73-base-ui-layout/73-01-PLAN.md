---
phase: 73-base-ui-layout
plan: 01
status: planned
started: null
completed: null
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/UI/Card.tsx
  - src/renderer/src/components/UI/Card.css
  - src/renderer/src/components/UI/Badge.tsx
  - src/renderer/src/components/UI/Badge.css
  - src/renderer/src/components/UI/Dialog.tsx
  - src/renderer/src/components/UI/Dialog.css
  - src/renderer/src/components/UI/Tabs.tsx
  - src/renderer/src/components/UI/Tabs.css
  - src/renderer/src/components/UI/Tooltip.tsx
  - src/renderer/src/components/UI/Tooltip.css
  - src/renderer/src/components/UI/index.ts
autonomous: true
must_haves:
  truths:
    - "Card provides glass-effect container with optional border glow"
    - "Badge displays status with semantic colors"
    - "Dialog renders modal with backdrop blur"
    - "Tabs allow switching between content panels"
    - "Tooltip shows on hover with positioning"
  artifacts:
    - path: "src/renderer/src/components/UI/Card.tsx"
      provides: "Card container component with glass effect"
    - path: "src/renderer/src/components/UI/Badge.tsx"
      provides: "Badge component for statuses"
    - path: "src/renderer/src/components/UI/Dialog.tsx"
      provides: "Modal dialog with backdrop"
    - path: "src/renderer/src/components/UI/Tabs.tsx"
      provides: "Tab navigation component"
    - path: "src/renderer/src/components/UI/Tooltip.tsx"
      provides: "Hover tooltip component"
  key_links:
    - from: "src/renderer/src/components/UI/index.ts"
      to: "All layout components"
      via: "Barrel export"
---

<objective>
Create layout components (Card, Badge, Dialog, Tabs, Tooltip) with synthwave styling using CSS custom properties from Phase 70.

Purpose: Complete the layout primitives for the UI component library, enabling consistent containers, modals, and navigation elements.
Output: Five new layout components with CSS files, added to barrel export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v2.6-ROADMAP.md

# Prior phases provide CSS custom properties and patterns
@.planning/phases/70-theme-infrastructure/70-01-SUMMARY.md
@.planning/phases/71-base-ui-buttons/71-01-SUMMARY.md
@.planning/phases/72-base-ui-controls/72-01-SUMMARY.md

# Reference files for patterns
@src/renderer/src/themes/synthwave.css
@src/renderer/src/components/UI/Button.tsx
@src/renderer/src/components/UI/Button.css
@src/renderer/src/components/UI/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Component</name>
  <files>src/renderer/src/components/UI/Card.tsx, src/renderer/src/components/UI/Card.css</files>
  <action>
Create Card component with glass effect styling:

**CSS (Card.css):**
- Glass effect: `backdrop-filter: blur(10px)`, semi-transparent background (--bg-surface)
- Default border: subtle magenta (--border-default)
- Hover variant: border glow effect (--glow-border-secondary)
- Elevated variant: stronger shadow and background (--bg-elevated)
- Clickable variant: cursor pointer, hover states
- Padding sizes: sm/md/lg using --space-* variables
- Border radius using --radius-md

**Component (Card.tsx):**
```typescript
interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'elevated' | 'outline';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  hoverable?: boolean;
  clickable?: boolean;
}
```
- forwardRef for DOM access
- Compound components: Card.Header, Card.Body, Card.Footer (optional)
- Focus-visible outline when clickable

Reference doc/2.png: Kanban cards have glass effect with subtle borders.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Card component renders with glass effect, border glow on hover works</done>
</task>

<task type="auto">
  <name>Task 2: Badge Component</name>
  <files>src/renderer/src/components/UI/Badge.tsx, src/renderer/src/components/UI/Badge.css</files>
  <action>
Create Badge component for status indicators:

**CSS (Badge.css):**
- Base: small rounded pill shape (--radius-full)
- Size variants: sm (smaller text), md (default)
- Status variants using semantic colors:
  - success: --badge-success-bg, --badge-success-text
  - warning: --badge-progress-bg, --badge-progress-text
  - error: --badge-error-bg, --badge-error-text
  - info: --badge-blocked-bg, --badge-blocked-text
  - default: neutral styling
- Optional dot indicator (small circle before text)
- Text should not wrap, truncate if needed

**Component (Badge.tsx):**
```typescript
interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'default' | 'success' | 'warning' | 'error' | 'info';
  size?: 'sm' | 'md';
  dot?: boolean;
}
```
- Simple span element
- Icon support (optional)

Reference doc/1.png: badges show status colors (cyan for info, green for success, etc).
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Badge component renders with 5 variants, dot indicator optional</done>
</task>

<task type="auto">
  <name>Task 3: Dialog Component</name>
  <files>src/renderer/src/components/UI/Dialog.tsx, src/renderer/src/components/UI/Dialog.css</files>
  <action>
Create Dialog (modal) component with backdrop:

**CSS (Dialog.css):**
- Backdrop: semi-transparent dark (--dialog-backdrop), blur effect
- Dialog panel: elevated background (--dialog-bg), magenta border (--dialog-border)
- Centered positioning with max-width
- Size variants: sm (400px), md (500px), lg (700px), full (90vw)
- Animation: fade in backdrop, scale up dialog
- Close button in top-right corner
- Header/body/footer sections with spacing

**Component (Dialog.tsx):**
```typescript
interface DialogProps {
  open: boolean;
  onClose: () => void;
  size?: 'sm' | 'md' | 'lg' | 'full';
  closeOnBackdrop?: boolean;
  closeOnEscape?: boolean;
  children: React.ReactNode;
}
```
- Use React Portal to render at document body
- Trap focus within dialog when open
- Close on Escape key (default true)
- Close on backdrop click (default true)
- Compound components: Dialog.Header, Dialog.Body, Dialog.Footer

Reference doc/3.png: dialogs have elevated dark background with magenta border.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Dialog opens with backdrop, closes on escape/backdrop click, focus trapped</done>
</task>

<task type="auto">
  <name>Task 4: Tabs Component</name>
  <files>src/renderer/src/components/UI/Tabs.tsx, src/renderer/src/components/UI/Tabs.css</files>
  <action>
Create Tabs component for tabbed navigation:

**CSS (Tabs.css):**
- Tab list: horizontal row with subtle bottom border
- Tab trigger: transparent background, muted text
- Active tab: cyan text (--accent-primary), bottom border indicator
- Hover: text brightens, subtle background
- Focus-visible outline
- Tab panel: hidden when inactive, shown when active
- Variant: underline (default), pills (rounded backgrounds)

**Component (Tabs.tsx):**
```typescript
interface TabsProps {
  defaultValue?: string;
  value?: string;
  onValueChange?: (value: string) => void;
  children: React.ReactNode;
  variant?: 'underline' | 'pills';
}

interface TabsListProps {
  children: React.ReactNode;
}

interface TabsTriggerProps {
  value: string;
  children: React.ReactNode;
  disabled?: boolean;
}

interface TabsContentProps {
  value: string;
  children: React.ReactNode;
}
```
- TabsContext for state management
- Controlled/uncontrolled support
- Keyboard navigation (arrow keys between tabs)
- Compound components: Tabs, Tabs.List, Tabs.Trigger, Tabs.Content

Reference doc/2.png: feature tabs at top of DAG view use underline style.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Tabs switch content, keyboard navigation works, active indicator shows</done>
</task>

<task type="auto">
  <name>Task 5: Tooltip Component and Barrel Export</name>
  <files>src/renderer/src/components/UI/Tooltip.tsx, src/renderer/src/components/UI/Tooltip.css, src/renderer/src/components/UI/index.ts</files>
  <action>
Create Tooltip component and update barrel export:

**CSS (Tooltip.css):**
- Tooltip: elevated background (--bg-elevated), subtle border
- Arrow pointing to trigger element
- Positions: top (default), bottom, left, right
- Max-width for long content
- Animation: fade in with slight translate
- Z-index: --z-tooltip

**Component (Tooltip.tsx):**
```typescript
interface TooltipProps {
  content: React.ReactNode;
  children: React.ReactElement;
  position?: 'top' | 'bottom' | 'left' | 'right';
  delay?: number;
}
```
- Wrap trigger element, show tooltip on hover
- Delay before showing (default 200ms)
- Use absolute positioning relative to trigger
- Hide on mouse leave

**Update index.ts:**
Add exports for all new components:
- Card, CardProps
- Badge, BadgeProps
- Dialog, DialogProps
- Tabs, TabsProps, TabsListProps, TabsTriggerProps, TabsContentProps
- Tooltip, TooltipProps
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>Tooltip shows on hover, all layout components exported from index.ts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Card.tsx exists with glass effect and hover states
- [ ] Badge.tsx exists with 5 status variants
- [ ] Dialog.tsx opens/closes with backdrop and focus trap
- [ ] Tabs.tsx switches content with keyboard support
- [ ] Tooltip.tsx shows on hover with positioning
- [ ] All components use CSS custom properties (no hardcoded colors)
- [ ] All components exported from index.ts
</verification>

<success_criteria>

- All 5 layout components created with CSS files
- All components follow Phase 71/72 patterns (forwardRef where appropriate, CSS variables)
- Barrel export updated with all new components
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/73-base-ui-layout/73-01-SUMMARY.md`
</output>
