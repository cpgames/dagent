---
phase: 81-polish-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/Toast/Toast.tsx
  - src/renderer/src/components/Toast/Toast.css
  - src/renderer/src/components/ErrorBoundary/ErrorBoundary.tsx
  - src/renderer/src/components/ErrorBoundary/ErrorBoundary.css
  - src/renderer/src/components/Loading/LoadingSpinner.tsx
  - src/renderer/src/components/Loading/LoadingSpinner.css
  - src/renderer/src/themes/synthwave.css
autonomous: true
must_haves:
  truths:
    - "Toast notifications display with synthwave styling"
    - "Error boundary displays with synthwave styling"
    - "Loading spinners use theme colors"
    - "All three components use CSS custom properties"
  artifacts:
    - path: "src/renderer/src/components/Toast/Toast.css"
      provides: "Toast styling with theme variables"
    - path: "src/renderer/src/components/ErrorBoundary/ErrorBoundary.css"
      provides: "Error boundary styling with theme variables"
    - path: "src/renderer/src/components/Loading/LoadingSpinner.css"
      provides: "Loading spinner styling with theme variables"
  key_links:
    - from: "Toast.tsx"
      to: "Toast.css"
      via: "CSS import"
    - from: "ErrorBoundary.tsx"
      to: "ErrorBoundary.css"
      via: "CSS import"
    - from: "LoadingSpinner.tsx"
      to: "LoadingSpinner.css"
      via: "CSS import"
---

<objective>
Migrate Toast, ErrorBoundary, and LoadingSpinner components to use CSS custom properties from the synthwave theme.

Purpose: Complete the v2.6 Synthwave UI redesign by migrating the remaining utility components that still use hardcoded Tailwind color classes.

Output: Three components fully migrated with companion CSS files using theme variables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.6-ROADMAP.md

# Theme reference
@src/renderer/src/themes/synthwave.css

# Components to migrate
@src/renderer/src/components/Toast/Toast.tsx
@src/renderer/src/components/ErrorBoundary/ErrorBoundary.tsx
@src/renderer/src/components/Loading/LoadingSpinner.tsx

# Pattern reference (Button.css shows the established pattern)
@src/renderer/src/components/UI/Button.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Toast component to CSS custom properties</name>
  <files>src/renderer/src/components/Toast/Toast.tsx, src/renderer/src/components/Toast/Toast.css</files>
  <action>
Create Toast.css with synthwave styling:
- Container: fixed bottom-4 right-4, z-index using --z-toast
- Toast item: --bg-elevated background, --border-default border, rounded with --radius-md
- Type-specific colors using semantic tokens:
  - success: --color-success for icon/border glow
  - error: --color-error for icon/border glow
  - warning: --color-warning for icon/border glow
  - info: --accent-primary for icon/border glow
- Text: --text-primary for message
- Dismiss button: --text-secondary, hover --text-primary
- Add subtle glow effect on hover matching type color
- Entry animation: slide-in from right with fade

Update Toast.tsx:
- Import Toast.css
- Replace Tailwind color classes with BEM class names
- Keep role="alert" for accessibility
- Use semantic HTML structure
  </action>
  <verify>npm run build succeeds, Toast.css exists and is imported</verify>
  <done>Toast notifications display with synthwave colors, no hardcoded Tailwind colors remain</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ErrorBoundary component to CSS custom properties</name>
  <files>src/renderer/src/components/ErrorBoundary/ErrorBoundary.tsx, src/renderer/src/components/ErrorBoundary/ErrorBoundary.css</files>
  <action>
Create ErrorBoundary.css with synthwave styling:
- Full container: --bg-base background, centered content
- Error card: --bg-elevated background, --color-error border with glow
- Heading: --color-error text with subtle glow
- Message: --text-secondary
- Try Again button: use danger variant styling (--color-error bg)
- Add error icon with --color-error glow

Update ErrorBoundary.tsx:
- Import ErrorBoundary.css
- Replace Tailwind classes (bg-gray-900, bg-red-900/50, border-red-500, etc.) with BEM classes
- Keep semantic structure for accessibility
  </action>
  <verify>npm run build succeeds, ErrorBoundary.css exists and is imported</verify>
  <done>Error boundary displays with synthwave error styling, no hardcoded Tailwind colors remain</done>
</task>

<task type="auto">
  <name>Task 3: Migrate LoadingSpinner component to CSS custom properties</name>
  <files>src/renderer/src/components/Loading/LoadingSpinner.tsx, src/renderer/src/components/Loading/LoadingSpinner.css</files>
  <action>
Create LoadingSpinner.css with synthwave styling:
- Spinner: --accent-primary for the visible arc, --border-subtle for the track
- Three size variants (sm/md/lg) with appropriate dimensions
- Animation: smooth rotation with subtle glow pulse
- Message text: --text-secondary
- LoadingOverlay: --bg-overlay background with backdrop blur

Update LoadingSpinner.tsx:
- Import LoadingSpinner.css
- Replace Tailwind classes (border-gray-600, border-t-blue-500, bg-gray-900/80) with BEM classes
- Keep flex centering structure
  </action>
  <verify>npm run build succeeds, LoadingSpinner.css exists and is imported</verify>
  <done>Loading spinners display with synthwave cyan accent, no hardcoded Tailwind colors remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Toast.css, ErrorBoundary.css, LoadingSpinner.css all exist
- [ ] All three components import their CSS files
- [ ] No hardcoded Tailwind color classes (bg-gray-*, text-gray-*, border-*-500, etc.) remain in these components
- [ ] Components use CSS custom properties from synthwave.css
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Toast, ErrorBoundary, and LoadingSpinner use theme variables exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/81-polish-accessibility/81-01-SUMMARY.md`
</output>
